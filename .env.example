# Comma-separated Tavily API keys. TAVILY_API_KEY remains supported for one key.
TAVILY_API_KEYS=key-a,key-b

# Optional defaults forwarded to the search tool.
# DEFAULT_PARAMETERS={"search_depth":"basic","max_results":10}
